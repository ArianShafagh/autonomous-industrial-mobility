# colcon build artefacts
build/
install/
log/

# Python virtual environment (python3 -m venv --system-site-packages venv)
venv/

# Runtime state
*.db

# logs/, tools/ai/results/, tools/ai/checkpoints/ and tools/nav/results/ ARE tracked (since
# 2026-09-21): they hold the thesis evidence (every Gazebo run, every evaluation, the figures), and
# the thesis is written on a machine that cannot re-run the simulation. See THESIS_DATA.md.

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/

# Editor
.vscode/

# Windows alternate-data-stream metadata
*:Zone.Identifier

# Phantom entry created by the Claude Code Bash sandbox for the deleted old venv
robofetch_venv/
